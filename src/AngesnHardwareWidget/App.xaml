<Application x:Class="AngesnHardwareWidget.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Teal.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Light.Teal rather than Dark.Teal, because the widget is the only dark surface here
                 and it paints its own card colours explicitly in MainWindow.ApplyAppearance. With a
                 dark application theme, every control nobody had hand-styled inherited dark-theme
                 colours instead - which is how the settings dialog ended up with a scrollbar whose
                 hover state was a pale highlight meant for a dark background. A light app theme
                 makes the dialogs correct by default and leaves the widget unaffected. -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
                <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                <Setter Property="Padding" Value="9,3" />
                <Setter Property="MinHeight" Value="24" />
            </Style>

            <!-- The widget's readout: a label column and a right-aligned value column. -->
            <Style x:Key="MetricLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Opacity" Value="0.65" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,2,10,2" />
            </Style>
            <Style x:Key="MetricValue" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Consolas, Cascadia Mono, Segoe UI Mono" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,2,0,2" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
